To train your generators using the LSUN database, run the command:

```
th main.lua --dataset lsun
```

To train your generators using the Imagenet database, run the command:

```
th main.lua --dataset imagenet
```
